# Build output and generated files
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Dependencies and lockfiles
node_modules/
package-lock.json

# Static assets — hand-tuned/minified SVG gains nothing from reformatting
public/

# Machine-managed: Claude Code rewrites .claude/, and the agent-rules block in
# AGENTS.md is regenerated by `next dev` on every start. Formatting either one
# only creates churn.
.claude/
AGENTS.md
